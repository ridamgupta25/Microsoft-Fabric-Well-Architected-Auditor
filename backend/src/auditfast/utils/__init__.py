"""Small shared helpers.

Deliberately near-empty. A utils package attracts anything without an obvious
home and becomes a dependency magnet; prefer putting code next to what it serves
and only promote it here when a third caller appears.
"""
