"""MCP adapter.

Exposes the same services as the REST API and CLI as agent-callable tools.
Optional: needs ``pip install -e "backend[mcp]"``. The server module is not
imported here so the package stays importable without the extra installed.
"""
