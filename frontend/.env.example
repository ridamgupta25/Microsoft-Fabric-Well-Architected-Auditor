# Base URL of the backend API.
# Leave blank in development to use the Vite proxy (same-origin /api).
VITE_API_BASE_URL=

# Where the dev proxy forwards /api requests.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
